import { Select } from "./Select.d";

export class InfoSelectQuest extends Select {
  constructor(classSelect: string, selectText: string) {
    super(classSelect, selectText)
    super.selectOptions
    super.changeSelectOption
  }
}